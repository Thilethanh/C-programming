typedef enum Color {
  RED = 55,
  GREEN = 176,
  BLUE = 38,
} color_t;

/*
 * Sometimes, you don't just want to enumerate some names 
 * (where the underlying integer constant values don't really matter)... 
 * you want to set those enumerations to specific values.
 * /

