typedef enum Color{
	RED,
	GREEN,
	BLUE,
	
}color_t;

/*
 * In the example above, color_t is a new type 
 * that can only have one of the values defined in the enum:
 * RED, which is 0
 * GREEN, which is 1
 * BLUE, which is 2
 */

